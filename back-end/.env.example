# MongoDB Atlas Connection URI
# Replace with your actual MongoDB Atlas connection string
# Format: mongodb+srv://<username>:<password>@<cluster-url>/<database-name>?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/swapbay?retryWrites=true&w=majority

# JWT Secret Key
# Generate a secure random string for production use
# Example: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Server Port
PORT=3000

# Node Environment
NODE_ENV=development

# JWT Token Expiration (optional)
# Default: 7d (7 days)
# Examples: 1h, 24h, 7d, 30d
JWT_EXPIRES_IN=7d
